{% extends 'base.html.twig' %}

{% block title %}Edit Task{% endblock %}

{% block body %}
    <h1>Edit Task</h1>
    
    <form action="{{ path ('task_edit') }}">

    {{ include('task/_form.html.twig', {'button_label': 'Update'}) }}

    </form>

    <a href="{{ path('task_index') }}">back to list</a>

    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
